{
  "description": "Intent-Engine session initialization - runs ie status on session start",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/scripts/session-start.js",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
